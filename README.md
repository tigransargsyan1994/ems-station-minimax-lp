# ems-station-minimax-lp
Semestral project (CTU FTS): EMS station placement using a minimax (p-center style) mixed-integer linear program in Python (PuLP). Objective minimizes the worst-case travel time under a budget constraint and a mutual-exclusion rule.
